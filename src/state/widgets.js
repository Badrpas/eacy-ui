import WidgetStorage from '../structures/widget-storage';

const widgets = new WidgetStorage();

widgets.addWidget('menu/menu-widget');
// widgets.addWidget('heha');
// widgetStorage.addWidget('slkdjflk', {y: 150});

export default widgets;
